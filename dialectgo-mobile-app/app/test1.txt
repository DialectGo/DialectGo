import React from 'react';
import {Text, View, TextInput} from 'react-native';


const GetFullName = (
  FirstName: string,
  LastName: string,
  MiddleName: string,
) => {
  return FirstName + ' ' + MiddleName + '' + LastName;;
};
export default function Test() {
  const name = 'John Doe';
  return (
    <View
      style = {{
        flex: 1,
        justifyContent: 'center',
        alignItems: 'center'
      }}>
      <Text>
        Hello, I'm {GetFullName('John', 'Doe', 'Alcantara')}!
      </Text>
      <TextInput
        style = {{
          height: 30,
          width: 200,
          borderColor: 'blue',
          borderWidth: 1,
        }}
        defaultValue = "Type Here"
      />
    </View>
  );
}